# Session Archive — 2026-05-08

Self-contained snapshot of work, memory, and assets from the 2026-05-08 working session on the CityFurnish Ops Dashboard.

## What's here

```
_session_archive_2026-05-08/
├── README.md               ← you are here
├── CHAT_HISTORY.md         ← chronological log of session work
├── MEMORY.md               ← long-lived facts, conventions, architecture
├── SKILLS.md               ← reusable workflows + code patterns
├── CLAUDE.md.snapshot      ← snapshot of project-root CLAUDE.md as of session end
├── SKILLS.md.snapshot      ← snapshot of project-root SKILLS.md as of session end
├── README.md.snapshot      ← snapshot of project-root README.md as of session end
├── plans/
│   └── plan-cbm-and-benchmarks.md   ← deferred CBM + Benchmarks + Missing FSD plan
├── mockups/
│   └── kpi-trend-table-samples.html ← Option B mockup approved by user for KPI Trend
└── scripts/
    ├── refresh_from_metabase.py     ← Python refresh script snapshot
    └── cloudflare-worker.js         ← Cloudflare Worker snapshot
```

## Where to start

1. **Returning to this work later?** Read `MEMORY.md` first (architecture + conventions), then `CHAT_HISTORY.md` for what was done this session.
2. **Building a new feature on KPI Trend?** Read `SKILLS.md` "Adding a new comparison table" first.
3. **Continuing the deferred CBM / Benchmarks work?** See `plans/plan-cbm-and-benchmarks.md`.

## Project root files

The live project memory files at `../CLAUDE.md` and `../SKILLS.md` are the source of truth. The snapshots in this folder are point-in-time references.
